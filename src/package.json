{
  "name": "some-parcel-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && rm -rf .cache && NODE_ENV=production && parcel build index.html --no-source-maps --detailed-report"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "1.12.4"
  }
}
