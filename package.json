{
  "name": "github-rebuild-push",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd src && rm -rf dist && rm -rf .cache && npm run build && exit 0"
  },
  "author": "",
  "license": "ISC"
}
